import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nossahistoria.app',
  appName: 'Nossa História de Amor',
  webDir: 'dist'
};

export default config;
